AI Tool: ChatGPT

Prompt/Request:
I asked for help evaluating the design of the given `PriceCalculator` class and refactoring it to use the Strategy Pattern, including the required Java classes, driver, and markdown documentation.

How it helped:
It helped me organize the solution into a Strategy interface, separate strategy classes for each pricing rule, a context class (`PriceCalculator`), and a driver program that demonstrates the required outputs.